import React from 'react'
import ButtonElement, { IButtonElement } from '../../../../elements/button/ButtonElement'

const ButtonCellElement = (props: IButtonElement) => (
    <ButtonElement {...props} />
)

export default ButtonCellElement
